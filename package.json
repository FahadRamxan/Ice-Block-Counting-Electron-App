{
  "name": "ice-factory-block-counter",
  "version": "1.0.1",
  "description": "Ice block counting desktop app with multi-NVR support",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "npm run build:electron && concurrently \"npm run dev:react\" \"wait-on http://localhost:3000 && electron .\"",
    "dev:react": "vite",
    "dev:electron": "wait-on http://localhost:3000 && electron .",
    "build": "npm run build:react && npm run build:electron",
    "build:react": "vite build",
    "build:electron": "tsc -p src/electron/tsconfig.json",
    "preview": "vite preview",
    "dist": "npm run build && electron-builder",
    "setup:python-runtime": "powershell -ExecutionPolicy Bypass -File scripts/setup-embed-python.ps1",
    "dist:win": "npm run build && node scripts/assert-python-runtime.cjs && electron-builder --win",
    "release:patch": "npm version patch --no-git-tag-version && npm run dist:win",
    "release:minor": "npm version minor --no-git-tag-version && npm run dist:win",
    "release:major": "npm version major --no-git-tag-version && npm run dist:win"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "concurrently": "^9.0.1",
    "electron": "^33.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.1",
    "wait-on": "^7.2.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.icefactory.blockcounter",
    "productName": "Ice Factory Block Counter",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist-electron/**/*",
      "dist-react/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "backend",
        "to": "backend",
        "filter": [
          "**/*",
          "!**/data/outputs/**",
          "!**/__pycache__/**",
          "!**/*.pyc"
        ]
      },
      {
        "from": "python-runtime",
        "to": "python-runtime",
        "filter": [
          "**/*",
          "!**/__pycache__/**",
          "!**/*.pyc"
        ]
      },
      {
        "from": ".",
        "to": ".",
        "filter": [
          "*.pt"
        ]
      }
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  }
}
