node_modules/
__pycache__/
*.pyc
*.pyo

# Model runs — large binaries (GitHub limit 100MB)
backend/data/outputs/
*.avi

# Packaged Electron apps (hundreds of MB — build locally with npm run dist:win)
release/
release-build/

# Local DB + secrets (regenerated locally)
backend/data/ice_factory.db
backend/data/credentials.txt
